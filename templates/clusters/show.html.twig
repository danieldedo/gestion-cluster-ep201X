{% extends 'base.html.twig' %}

{% block title %}Clusters{% endblock %}

{% block body %}
    <h1>Clusters</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cluster.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ cluster.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_clusters_index') }}">back to list</a>

    <a href="{{ path('app_clusters_edit', {'id': cluster.id}) }}">edit</a>

    {{ include('clusters/_delete_form.html.twig') }}
{% endblock %}
